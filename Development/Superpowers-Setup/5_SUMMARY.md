# Document 5: Summary

## Context

- **Playbook**: Superpowers Setup
- **Agent**: {{AGENT_NAME}}
- **Project**: {{AGENT_PATH}}
- **Date**: {{DATE}}
- **Working Folder**: {{AUTORUN_FOLDER}}

## Purpose

Produce a single human-facing document that says, in plain language, what was installed, what (if anything) the user still needs to do, and where to learn more. This is the only file the user is expected to read.

## Inputs

- `{{AUTORUN_FOLDER}}/PROVIDER.md`
- `{{AUTORUN_FOLDER}}/INSTALL_PLAN.md`
- `{{AUTORUN_FOLDER}}/INSTALL_LOG.md`
- `{{AUTORUN_FOLDER}}/VERIFY.md`
- `{{AUTORUN_FOLDER}}/USER_ACTIONS.md` (may not exist)

## Tasks

### Task 1: Pull the headline facts

- [ ] **Read all input files** and extract:
  - Provider value
  - Install outcome (`AUTOMATED_COMPLETE`, `AWAITING_USER`, `FAILED`, `SKIPPED`)
  - Verification verdict (`INSTALLED-AND-ACTIVE`, `INSTALLED-PENDING-RELOAD`, `PENDING-USER-ACTION`, `FAILED`, `SKIPPED`)
  - Whether `USER_ACTIONS.md` exists and is non-empty

### Task 2: Write `SUPERPOWERS_SETUP.md`

- [ ] **Write `{{AUTORUN_FOLDER}}/SUPERPOWERS_SETUP.md`**:

  ```markdown
  # Superpowers Setup — Summary

  **Agent**: {{AGENT_NAME}}
  **Provider**: <detected provider>
  **Date**: {{DATE}}
  **Status**: <Installed | Installed (restart required) | Action required | Failed | Skipped>

  ---

  ## What is Superpowers?

  [Superpowers](https://github.com/obra/superpowers) is a software-development methodology delivered to coding agents as a bundle of composable skills (brainstorming, writing-plans, using-git-worktrees, test-driven-development, requesting-code-review, finishing-a-development-branch, and more) plus initial instructions that make the agent use them automatically. Skills trigger based on context, so the agent gains a built-in TDD/YAGNI/DRY workflow and can run autonomously for longer without losing the plan.

  ## What this playbook did

  <2-4 sentences. Plain language. Mention the provider it detected, the install path it took, and whether anything was actually changed on disk.>

  ## What you need to do next

  <Pick the matching block:>

  ### If status is "Installed" or "Installed (restart required)"
  - <If restart required:> Restart your <provider> session. New sessions will have Superpowers loaded.
  - In your next session, ask: `Tell me about your superpowers`. The agent should describe its bundled skills.
  - Try the entry-point skill: ask the agent to brainstorm a feature, and watch it walk through the design → plan → execute workflow.

  ### If status is "Action required"
  - Open `{{AUTORUN_FOLDER}}/USER_ACTIONS.md` and follow the steps in order. They are short — typically one or two slash commands to paste into your <provider> session.
  - After completing them, ask: `Tell me about your superpowers` to confirm.

  ### If status is "Failed"
  - The failure point is recorded in `{{AUTORUN_FOLDER}}/INSTALL_LOG.md`. Open that file for the exact command and error.
  - Common causes: missing harness CLI, network failure during marketplace add, permission issues writing config files.

  ### If status is "Skipped"
  - Reason is in `{{AUTORUN_FOLDER}}/INSTALL_PLAN.md` under the **Skip / Block** section. Most likely your provider does not have a documented Superpowers install path yet — check <https://github.com/obra/superpowers> for the latest list of supported harnesses.

  ## Files generated by this playbook

  | File | Purpose |
  |---|---|
  | `PROVIDER.md` | Detected harness and confidence |
  | `INSTALL_PLAN.md` | Concrete plan, classified into automatable vs user-required |
  | `INSTALL_LOG.md` | Result of executing the automatable steps |
  | `USER_ACTIONS.md` | Paste-ready instructions for any user-required steps (only if needed) |
  | `VERIFY.md` | Verification result |
  | `SUPERPOWERS_SETUP.md` | This summary |

  ## Reference

  - Superpowers repo: <https://github.com/obra/superpowers>
  - Maestro Playbooks: <https://github.com/RunMaestro/Maestro-Playbooks>
  ```

### Task 3: Surface the result

- [ ] **State the outcome plainly** in your final response in this session: which provider was detected, the status (Installed / Action required / Failed / Skipped), and the path to `SUPERPOWERS_SETUP.md`. Keep it to ~3 sentences. The user will read this in their session log.

## Success Criteria

- `SUPERPOWERS_SETUP.md` exists and includes only the block matching the actual outcome (do not include all four "What you need to do next" branches).
- Status field at the top is one of the five canonical values.
- The summary references the generated files by absolute or `{{AUTORUN_FOLDER}}/`-relative path so the user can open them.

## Status

Mark complete when `SUPERPOWERS_SETUP.md` is written and the outcome is summarized in the session.

---

**Playbook complete.** No looping.
